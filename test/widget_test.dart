import 'package:flutter_test/flutter_test.dart';

// Replace "my_widget.dart" with the actual path to your widget file

void main() {
  test('MyWidget displays text correctly', () {
    const int firstValue = 1;
    const int secondValue = 2;

    expect(firstValue + secondValue, firstValue + secondValue);
  });
}
