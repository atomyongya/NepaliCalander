import 'package:flutter/material.dart';

class HorizontalTextView extends StatelessWidget {
  const HorizontalTextView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
